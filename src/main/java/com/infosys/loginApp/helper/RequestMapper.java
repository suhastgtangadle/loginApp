/*package com.infosys.loginApp.helper;

import org.json.JSONObject;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;

public class RequestMapper {


		  /**
		   * Method to map request
		   * 
		   * @param requestData JsonNode
		   * @param obj Class<T>
		   * @exception RuntimeException
		   * @return <T>
		   */
	/*	  public static <T> Object mapRequest(JSONObject requestData, Class<T> obj) throws RuntimeException {

		    try {
		      obj = new ObjectMapper().readValue(requestData, obj.class);
		    } catch (Exception e) {
		      e.printStackTrace();
		    }
		  }

		}

*/
